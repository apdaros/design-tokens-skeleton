{
  "name": "design-tokens",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "files": [
    "build",
    "properties"
  ],
  "scripts": {
    "build": "node ./tokens.js && node ./build.js",
    "tokens": "node ./tokens.js",
    "clean": "rm -rf build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run build && git add build"
    }
  },
  "devDependencies": {
    "husky": "^3.0.8",
    "style-dictionary": "2.8.2"
  },
  "dependencies": {
    "glob": "^7.1.4"
  }
}
